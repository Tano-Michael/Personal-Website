# Security Spec

Data Invariants:
- The site data is public for reading.
- Only an admin can manage the books, articles, podcasts, and configuration.

The "Dirty Dozen" Payloads:
- Not needed for read-only app basically, but for admin ops: Admin has to be authenticated.
